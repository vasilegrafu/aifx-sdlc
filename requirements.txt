# Everything needed to run what is in this repository.
#
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)   |   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Nothing yet. Every script here runs on the standard library, which is what
# lets a skill be copied into another checkout and still work.
#
# Anything that reaches the network, or renders a template, gets added the day
# something imports it -- not before. A dependency nothing imports is a
# dependency nobody maintains, and jinja2 sat here proving that until it was
# removed.

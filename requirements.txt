# Everything needed to run what is in this repository, in one venv at the root.
#
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)   |   source .venv/bin/activate
#   pip install -r requirements.txt
#
# The skills under .claude/skills/ need none of it. They import nothing but the
# standard library, deliberately: that is what lets one be copied into another
# checkout and still work. Everything below exists to run what they generate.

SQLAlchemy>=2.0
pytest>=8.0

# Not the solution's choices -- devfx's. It is linked in as a junction rather
# than installed, so it brings no metadata with it and what it imports has to be
# declared here by hand. Both were found by importing devfx.database.sqlalchemy
# until it stopped raising:
#
#   pydantic  <- devfx.data_retrieval_specs, used by StandardDbCtrl
#   pandas    <- devfx.core, reached through devfx's SessionMaker
#
# Anything else devfx grows will surface the same way, at import time.
pydantic>=2.0
pandas>=2.0

# SQLite needs no driver -- sqlite3 is in the standard library. A different
# database would add its driver here the day the configuration points at one.

# The web API layer. `httpx` is not a runtime dependency of the API itself: it
# is what FastAPI's TestClient is built on, and it is listed because without it
# the endpoints can only be imported, never exercised -- which for an API layer
# is the difference between generated and proven.
fastapi>=0.115
uvicorn>=0.32
httpx>=0.27

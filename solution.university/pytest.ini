[pytest]
pythonpath = .
# The source codebase uses `testpaths = .`; this application cannot. `devfx` is
# a junction to another repository, and collecting from `.` walks all of it.
testpaths = database_tests logic_tests
python_files = *_test.py test_*.py
python_classes = Test*
python_functions = test_*
